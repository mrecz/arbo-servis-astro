---
import { PhotoGalleryRuntimeClient } from './photo-gallery-runtime-client.tsx';
---

<div class="w-full mb-16">
  <!-- Gallery Header -->
  <div class="text-center mb-12">
    <div class="relative inline-block">
      <!-- Decorative elements -->
      <div class="absolute -top-4 -left-4 w-8 h-8 bg-carmine-100 rounded-full opacity-60"></div>
      <div class="absolute -bottom-2 -right-2 w-6 h-6 bg-latte-100 rounded-full opacity-80"></div>
      <div
        class="absolute top-1/2 -right-6 w-4 h-4 bg-carmine-200 rounded-full opacity-50 transform -translate-y-1/2"
      >
      </div>

      <!-- Main heading -->
      <h2
        class="font-montserrat relative text-4xl md:text-5xl font-bold text-stone-800 mb-4 leading-tight"
      >
        <span class="bg-gradient-to-r from-carmine-600 to-latte-600 bg-clip-text text-transparent">
          Podívejte se na některé z našich prací
        </span>
      </h2>

      <!-- Subtitle -->
      <p class="text-lg text-stone-600 max-w-2xl mx-auto leading-relaxed">
        Prohlédněte si naši galerii a nechte se inspirovat kvalitou a profesionálním přístupem k
        arboristickým službám
      </p>
    </div>

    <!-- Decorative line with gradient -->
    <div class="flex items-center justify-center mt-8">
      <div class="h-px bg-gradient-to-r from-transparent via-carmine-300 to-transparent w-32"></div>
      <div class="mx-4 w-2 h-2 bg-carmine-500 rounded-full"></div>
      <div class="h-px bg-gradient-to-r from-transparent via-latte-300 to-transparent w-32"></div>
    </div>
  </div>

  <!-- Gallery Container -->
  <div class="bg-latte-100 rounded-2xl shadow-xl border border-stone-100 overflow-hidden p-4">
    <PhotoGalleryRuntimeClient client:load />
  </div>
</div>
